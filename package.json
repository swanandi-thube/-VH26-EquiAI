{
  "name": "adaptivecache-platform",
  "version": "1.0.0",
  "description": "AdaptiveCache - Application-Aware Intelligent Caching & Backend Protection Platform",
  "private": true,
  "scripts": {
    "install:backend": "npm --prefix backend install",
    "install:frontend": "npm --prefix frontend install",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"cyan,magenta\" \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build:frontend": "npm --prefix frontend run build",
    "build:backend": "npm --prefix backend run build",
    "build": "npm run build:backend && npm run build:frontend",
    "start": "node backend/dist/server.js",
    "test": "npm --prefix backend test"
  },
  "dependencies": {},
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "cache",
    "adaptive-caching",
    "lru",
    "lfu",
    "gds",
    "redis",
    "postgresql",
    "observability",
    "backend-protection",
    "telemetry"
  ],
  "author": "AdaptiveCache Team",
  "license": "MIT"
}
